package com.sunforge.logic;

import java.awt.image.BufferedImage;
import java.util.HashMap;

public class HeaderCreator {
    public static BufferedImage createHeader(Card previewCard, String givenTitle, String givenMetaData, HashMap<String, HashMap<String, Integer>> givenStats){

        //TODO Adjust the numbers
        BufferedImage headerImage = new BufferedImage(3000, 400, 1);

        return headerImage;
    }
}
